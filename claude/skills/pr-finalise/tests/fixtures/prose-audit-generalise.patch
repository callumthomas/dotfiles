diff --git a/cmd/report/main.go b/cmd/report/main.go
index 1a2b3c4..5d6e7f8 100644
--- a/cmd/report/main.go
+++ b/cmd/report/main.go
@@ -12,0 +13,9 @@ import (
+// FetchHoldings returns the holdings for an account.
+func FetchHoldings(id string) ([]Holding, error) {
+	// Retry once because the upstream API is flaky at cold start.
+	rows, err := query(id)
+	if err != nil {
+		return nil, err
+	}
+	return rows, nil
+}
diff --git a/src/lib.rs b/src/lib.rs
new file mode 100644
index 0000000..9a8b7c6
--- /dev/null
+++ b/src/lib.rs
@@ -0,0 +1,7 @@
+//! Holdings aggregation helpers.
+
+/// Sums the holdings for one account.
+pub fn sum_holdings(rows: &[Row]) -> u64 {
+    // TODO handle negative balances
+    rows.iter().map(|r| r.amount).sum()
+}
diff --git a/Dockerfile b/Dockerfile
new file mode 100644
index 0000000..3c4d5e6
--- /dev/null
+++ b/Dockerfile
@@ -0,0 +1,7 @@
+# syntax=docker/dockerfile:1
+FROM node:22-alpine
+# Install deps before copying source so the layer caches.
+COPY package.json ./
+RUN npm ci
+COPY . .
+CMD ["node", "server.js"]
diff --git a/web/src/HoldingsPanel.jsx b/web/src/HoldingsPanel.jsx
index 2b3c4d5..6e7f8a9 100644
--- a/web/src/HoldingsPanel.jsx
+++ b/web/src/HoldingsPanel.jsx
@@ -18,0 +19,9 @@ export function HoldingsPanel({ id }) {
+  // eslint-disable-next-line react-hooks/exhaustive-deps
+  useEffect(() => { load(id) }, [])
+
+  return (
+    <div className="panel">
+      {/* Skeleton keeps the layout from jumping while data loads. */}
+      {loading && <Skeleton rows={3} />}
+    </div>
+  )
diff --git a/app/Models/Holding.php b/app/Models/Holding.php
new file mode 100644
index 0000000..4f5a6b7
--- /dev/null
+++ b/app/Models/Holding.php
@@ -0,0 +1,29 @@
+<?php
+
+declare(strict_types=1);
+
+namespace App\Models;
+
+use Illuminate\Database\Eloquent\Model;
+
+/**
+ * Holdings aggregated per account.
+ *
+ * @property int $account_id
+ */
+#[\AllowDynamicProperties]
+class Holding extends Model
+{
+    // phpcs:ignore Generic.Files.LineLength.TooLong
+    // The scope excludes accounts closed before the 2020 migration.
+    public function scopeActive($query)
+    {
+        return $query->whereNull('closed_at');
+    }
+
+    /** @var array<int, string> */
+    protected $fillable = ['account_id', 'amount'];
+
+    // protected $casts = ['closed_at' => 'datetime'];
+    // protected $dates = ['closed_at'];
+}
diff --git a/resources/views/holdings/index.blade.php b/resources/views/holdings/index.blade.php
new file mode 100644
index 0000000..5a6b7c8
--- /dev/null
+++ b/resources/views/holdings/index.blade.php
@@ -0,0 +1,10 @@
+@extends('layouts.app')
+
+{{-- Reviewers: the table below mirrors the legacy report layout. --}}
+@section('content')
+    <table class="holdings">
+        @foreach ($holdings as $holding)
+            <tr><td>{{ $holding->account_id }}</td></tr>
+        @endforeach
+    </table>
+@endsection
diff --git a/infra/terraform/holdings.tf b/infra/terraform/holdings.tf
new file mode 100644
index 0000000..6b7c8d9
--- /dev/null
+++ b/infra/terraform/holdings.tf
@@ -0,0 +1,9 @@
+# tflint-ignore: terraform_required_providers
+terraform {
+  required_version = ">= 1.6"
+}
+
+# Bucket keeps thirty days of report snapshots for audit.
+resource "aws_s3_bucket" "reports" {
+  bucket = "delio-holdings-reports"
+}
diff --git a/helm/holdings/templates/_helpers.tpl b/helm/holdings/templates/_helpers.tpl
new file mode 100644
index 0000000..7c8d9e0
--- /dev/null
+++ b/helm/holdings/templates/_helpers.tpl
@@ -0,0 +1,4 @@
+{{/* Expand the chart name, truncated to the label length limit. */}}
+{{- define "holdings.name" -}}
+{{- .Chart.Name | trunc 63 | trimSuffix "-" -}}
+{{- end -}}
diff --git a/web/src/styles/panel.less b/web/src/styles/panel.less
new file mode 100644
index 0000000..8d9e0f1
--- /dev/null
+++ b/web/src/styles/panel.less
@@ -0,0 +1,5 @@
+// stylelint-disable selector-max-id
+#holdings-panel {
+  /* Padding matches the card grid so the panel lines up. */
+  padding: @card-gap;
+}
diff --git a/docs/runbook.md b/docs/runbook.md
index 7c8d9e0..1f2a3b4 100644
--- a/docs/runbook.md
+++ b/docs/runbook.md
@@ -40,0 +41,3 @@ Restart the worker pool before retrying.
+<!-- Reviewers: this section duplicates the on-call guide; merge them later. -->
+
+## Rollback
diff --git a/scripts/deploy.sh b/scripts/deploy.sh
new file mode 100644
index 0000000..8b9c0d1
--- /dev/null
+++ b/scripts/deploy.sh
@@ -0,0 +1,5 @@
+#!/usr/bin/env bash
+set -euo pipefail
+TAG="build#not-a-comment"
+curl -fsSL "https://example.com/api#frag" -o /dev/null
+echo "$TAG"
diff --git a/src/legacy_removed.py b/src/legacy_removed.py
index 5c6d7e8..9f0a1b2 100644
--- a/src/legacy_removed.py
+++ b/src/legacy_removed.py
@@ -7,3 +6,0 @@ def totals(rows):
-# This helper predates the reporting rewrite.
-def old_total(rows):
-    return sum(r["amount"] for r in rows)
