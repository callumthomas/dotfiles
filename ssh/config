# Host i-* mi-*
# ProxyCommand ~/.ssh/aws-ssm-ec2-proxy-command.sh %h %r %p

# Per-machine overrides (instance IDs, internal hostnames, etc.)
Include ~/.ssh/config.local


Match Final Exec "echo $TERM | grep -q xterm-ghostty" Host *
	SetEnv TERM=xterm-256color
